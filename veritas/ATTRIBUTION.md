# Attribution

Everything under `formal/`, `runtime/` and `synthesis/` was written by **Ryan Matheu**
(`rmatheu@umd.edu`, GitHub `rymatheu`), a coauthor of the IDDMBSE paper, and is copied here from
his two research repositories at the commits named below. The files keep their original contents;
the edits made to fit this layout are listed at the end.

## `formal/bt2automata/`

* Upstream: <https://github.com/rymatheu/hscc25-rp>, branch `linux-x86`
* Commit: `e63fa448b4b06cf137b9f0f462a4ae1021e19c4a` (2025-02-28)
* Authors: Ryan Matheu, with Aniruddh G. Puranic, John S. Baras and Calin Belta as coauthors of
  the paper it accompanies (BT2Automata, HSCC '25). The repository is the HSCC'25 repeatability
  package for that paper.
* Copied from `bt2ta/`: `bt2ta/bt2ta.py`, `leaf_node_templates.xml`.
* Not copied: `uppaal-5.0.0-linux64/` (the 38 MB UPPAAL binary distribution — redistribution is
  not permitted, see the README), `UPPAAL Instructions.pdf` (the upstream README states it
  contains a license key), and `main.py`, which is replaced by `demo.py` here.

## `runtime/tbt-monitor/`

* Upstream: the same repository and commit, directory `hscc25-pandagym/`
* Copied: `tbt_monitor.py`, `panda_specifications.py`, `main.py`, `tqc_pandp.zip` (the trained TQC
  policy, 24 MB).
* Not copied: `videos/` (the recorded episode videos; `main.py` regenerates them).
* The monitor implements the online behavior-tree monitoring of OMTBT (ECC 2025) by the same four
  authors.

## `synthesis/ltbt/`

* Upstream: <https://github.com/rymatheu/ltbt>, branch `main`
* Commit: `6f2be3f9dc521dc075131ed01fce20c79b8fc797` (2026-03-17)
* Author: Ryan Matheu. The repository carries no README and names no paper; its content matches
  Matheu, Baras and Belta, *Ternary Logic Encodings of Temporal Behavior Trees with Application to
  Control Synthesis*, arXiv:2604.12092, which is most likely its companion paper.
* Copied: `ltbt/boolean.py`, `ltbt/ternary.py`, `robot.py`, `robot2.py`, `multiagent.py`,
  `multiagent2.py`, `plot.py`, `plot2.py`, `plot3.py`, and the saved single-robot solutions
  `robot/{true,false,true_tern,false_tern}/{x,u,z}.npy` (68 KB, what `plot.py` reads).
* Not copied: `multiagent/` and its archived solution sets, the solver logs `log.txt`,
  `ma_log.txt` and `log_double_int_ma.txt` (their runtimes and objectives are quoted in the
  README instead), the rendered figures `trajectory_plot*.pdf`, `queue.pdf`, `queue.png`,
  `test.png`, the one-line `temp.sh`, and `__pycache__/`.

The code is released here under the repository's MIT license.

## Third-party dependencies

RTAMT (BSD-3-Clause) does the STL robustness computation in `runtime/tbt-monitor/`. pyuppaal
(MIT) reads and writes the UPPAAL XML in `formal/bt2automata/`. matplotlib (BSD) draws the
figures in `datadriven/`. Gurobi, used by `synthesis/ltbt/`, and UPPAAL, used by
`formal/verify.py` to check the generated models, are downloaded separately under the reader's
own license. See the README.

## Edits made to the copied files

Only two files were changed, both for paths and both listed here. No algorithm was rewritten and
no "generated by" banners were added.

* `formal/bt2automata/demo.py` — replaces the upstream `bt2ta/main.py`. The composition, the
  declarations, the system instantiation and the two queries are identical, and the generated
  `BT_converted.xml` is the upstream script's output plus one line: the flat-system 1.1 DOCTYPE
  declaration, which pyuppaal's writer leaves out and which this script puts back after saving
  (md5 `96b2e35c435ce771191351242e29cb77`, and `9f1da760f65674818d156546092e6118` with that
  line removed, which is the upstream output byte for byte). What else changed: the leaf
  template file and the output file are resolved relative to the script rather than the working
  directory, the output path can be given as an argument, and a docstring was added.
* `runtime/tbt-monitor/main.py` — the policy zip and the `videos/` directory are resolved relative
  to the script rather than the working directory, and a `--no-video` flag was added that skips
  the `RecordVideo` wrapper and the moviepy slow-motion pass, so the rollout runs with no display.
  Nothing else changed.

`runtime/tbt-monitor/demo_synthetic.py`, `formal/bt2automata/demo.py`, `formal/verify.py`,
`formal/sysml2uppaal/`, `datadriven/`, `runtime/stl-observer/`, `tests/` and this directory's
`README.md`, `ATTRIBUTION.md` and `pyproject.toml` were written for this release.
